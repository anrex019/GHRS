# 🔧 Consultation Form Fix - Deployment Instructions

## პრობლემის აღწერა
კონსულტაციის ფორმა არ მუშაობს პროდაქშენზე, რადგან:
1. Backend-ზე არ არის `/api/consultation` endpoint ხელმისაწვდომი
2. Backend უნდა გადაიტვირთოს Render-ზე

## ✅ გაკეთებული ცვლილებები

### Frontend (`/app/config/api.ts`)
- ✅ წაშლილია "დროებითი ფიქსი" რომელიც პროდაქშენზე ხსნიდა `/api/` პრეფიქსს
- ✅ დამატებულია axios interceptor რომელიც უზრუნველყოფს `/api/` პრეფიქსს
- ✅ დამატებულია დეტალური ლოგირება Footer.tsx-ში

### Backend (`/backend/package.json`)
- ✅ წაშლილია problematic `prebuild` სკრიპტი
- ✅ Backend წარმატებით დაბილდა

## 🚀 Deployment ნაბიჯები

### 1. Backend Deployment (Render)

#### Option A: Git Push (რეკომენდებული)
```bash
cd /Users/macbook/Desktop/GHRS
git add .
git commit -m "Fix consultation endpoint and remove prebuild script"
git push origin main
```

Render ავტომატურად დააკვირდება ცვლილებებს და გადატვირთავს backend-ს.

#### Option B: Manual Redeploy
1. გადადით Render Dashboard-ზე: https://dashboard.render.com
2. იპოვეთ `ghrs-backend` სერვისი
3. დააჭირეთ "Manual Deploy" → "Deploy latest commit"

### 2. Frontend Deployment (Vercel)

#### Option A: Git Push (რეკომენდებული)
```bash
cd /Users/macbook/Desktop/GHRS
git add .
git commit -m "Fix API configuration for consultation form"
git push origin main
```

Vercel ავტომატურად დააკვირდება ცვლილებებს და გადატვირთავს frontend-ს.

#### Option B: Manual Deploy
```bash
cd /Users/macbook/Desktop/GHRS
npm run build
# შემდეგ Vercel Dashboard-ზე manually deploy
```

## 🧪 ტესტირება

### ლოკალური ტესტი
```bash
# Backend
cd /Users/macbook/Desktop/GHRS/backend
npm run start:dev

# Frontend (ახალ terminal-ში)
cd /Users/macbook/Desktop/GHRS
npm run dev
```

### Production ტესტი (curl)
```bash
curl -X POST https://ghrs-backend.onrender.com/api/consultation \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "phone": "+995555123456",
    "email": "test@example.com",
    "locale": "ka"
  }'
```

წარმატების შემთხვევაში უნდა დაბრუნდეს:
```json
{
  "_id": "...",
  "name": "Test User",
  "phone": "+995555123456",
  "email": "test@example.com",
  "locale": "ka",
  "status": "pending",
  "createdAt": "...",
  "updatedAt": "..."
}
```

## 📝 შემოწმების Checklist

- [ ] Backend დაბილდა წარმატებით
- [ ] Frontend დაბილდა წარმატებით
- [ ] Git-ში დაკომიტებულია ყველა ცვლილება
- [ ] Backend გადატვირთულია Render-ზე
- [ ] Frontend გადატვირთულია Vercel-ზე
- [ ] curl ტესტი მუშაობს
- [ ] ვებსაიტზე ფორმა მუშაობს

## 🔍 Debug ინფორმაცია

თუ პრობლემა კვლავ გრძელდება:

1. **შეამოწმეთ Backend Logs (Render)**
   - გადადით Render Dashboard → ghrs-backend → Logs
   - ეძებეთ შეცდომები startup-ის დროს

2. **შეამოწმეთ Frontend Console**
   - გახსენით Browser DevTools → Console
   - ეძებეთ ლოგები: "🔍 Consultation Request Details"

3. **შეამოწმეთ Network Tab**
   - Browser DevTools → Network
   - გაგზავნეთ ფორმა და ნახეთ რა URL-ზე მიდის request

## 📞 მნიშვნელოვანი URL-ები

- **Backend API**: https://ghrs-backend.onrender.com
- **Frontend**: https://ghrs-group.com (ან თქვენი Vercel URL)
- **Test Endpoint**: https://ghrs-backend.onrender.com/api/consultation

## ✨ მოსალოდნელი შედეგი

მას შემდეგ რაც backend და frontend გადაიტვირთება:
- ✅ კონსულტაციის ფორმა იმუშავებს პროდაქშენზე
- ✅ მომხმარებლები მიიღებენ confirmation email-ს
- ✅ ადმინები მიიღებენ notification email-ს
- ✅ მონაცემები შეინახება MongoDB-ში
